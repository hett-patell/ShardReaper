---
name: surface
description: Ranked attack surface — P1 (start here), P2 (after), kill list (gate-killed findings). Usage: /surface <dir>
---

# /surface — where the doors are, ranked

## Output

- **P1 — start here**: high/critical findings from recon
- **P2 — after**: open unusual services, live web apps, info-level intel
- **KILL LIST**: findings the 7-Question Gate already killed — never re-try

## Usage

```
shardreaper surface eng/
```

Pairs with `/plan` (technique-level fire orders) and `/chain` (impact chains).
