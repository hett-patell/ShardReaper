#!/usr/bin/env python3
"""ShardReaper launcher — works without installation."""
import os
import sys

sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))

from shardreaper.cli import main  # noqa: E402

if __name__ == "__main__":
    raise SystemExit(main())
